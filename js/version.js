// Single source of truth for the app version (semver).
// Bump this in every PR that changes app files — CI enforces it, and the
// deploy workflow tags a matching vX.Y.Z release on main.
const APP_VERSION = "0.1.0";
